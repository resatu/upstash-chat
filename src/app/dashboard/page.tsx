import {FC} from 'react';

interface PageProps {}

const page : FC<PageProps> = ({}) => {
    return (
        <div>
            <button> Hello World</button>
        </div>
    );
}

export default page;